---
title: 'Primeiros Passos'
description: 'Configure sua primeira integração com o Manager SaaS em minutos'
---

# Primeiros Passos - Manager SaaS

Configure sua primeira integração com o Manager SaaS em poucos minutos e comece a emitir documentos fiscais eletrônicos.

## Pré-requisitos

### **Conta Manager SaaS**
- Conta ativa no Manager SaaS
- Token de acesso válido
- Configurações de ambiente configuradas

### **Dados da Empresa**
- CNPJ/CPF válido
- Inscrição estadual ativa
- Certificado digital (se aplicável)
- Dados de endereço completos

### **Ambiente de Desenvolvimento**
- Editor de código ou IDE
- Cliente HTTP (cURL, Postman, Insomnia)
- Conhecimento básico de APIs REST

## Passo 1: Obter Credenciais

### **1.1 Acesse o Dashboard**
1. Acesse [dashboard.managersaas.com](https://dashboard.managersaas.com)
2. Faça login com suas credenciais
3. Navegue para a seção "API Keys"

### **1.2 Gere seu Token**
1. Clique em "Gerar Nova Chave"
2. Escolha um nome para identificação
3. Selecione as permissões necessárias
4. Copie o token gerado

<Note>
⚠️ **Importante**: Guarde seu token em local seguro. Ele não será exibido novamente.
</Note>

## Passo 2: Configurar o Ambiente

### **2.1 Variáveis de Ambiente**

Crie um arquivo `.env` com suas credenciais:

```bash
# Manager SaaS Configuration
MANAGER_SAAS_API_URL=https://api.managersaas.com/v1
MANAGER_SAAS_TOKEN=seu_token_aqui
MANAGER_SAAS_ENVIRONMENT=production
```

### **2.2 Configuração do Cliente HTTP**

Configure o Postman ou Insomnia:

- **Base URL**: `https://api.managersaas.com/v1`
- **Authorization**: `Bearer SEU_TOKEN`
- **Content-Type**: `application/json`

## Passo 3: Primeira Requisição

### **3.1 Teste de Conexão**

Vamos testar se sua conexão está funcionando:

```bash
curl -X GET "https://api.managersaas.com/v1/status" \
  -H "Authorization: Bearer SEU_TOKEN" \
  -H "Content-Type: application/json"
```

**Resposta esperada:**
```json
{
  "status": "success",
  "message": "API funcionando normalmente",
  "timestamp": "2024-01-15T10:30:00Z",
  "version": "1.0.0"
}
```

### **3.2 Consulta de Configurações**

Verifique suas configurações de empresa:

```bash
curl -X GET "https://api.managersaas.com/v1/empresa/configuracoes" \
  -H "Authorization: Bearer SEU_TOKEN"
```

## Passo 4: Emitir Primeiro Documento

### **4.1 Emissão de CT-e OS**

Vamos emitir um CT-e OS simples para teste:

```bash
curl -X POST "https://api.managersaas.com/v1/cte/os/emitir" \
  -H "Authorization: Bearer SEU_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "tipo_documento": "CTE_OS",
    "emissor": {
      "cnpj": "12345678000199",
      "inscricao_estadual": "123456789",
      "razao_social": "SUA EMPRESA LTDA",
      "endereco": {
        "logradouro": "Rua Exemplo, 123",
        "bairro": "Centro",
        "municipio": "São Paulo",
        "uf": "SP",
        "cep": "01234-567"
      }
    },
    "tomador": {
      "cnpj": "98765432000188",
      "razao_social": "CLIENTE TESTE LTDA",
      "endereco": {
        "logradouro": "Av. Teste, 1000",
        "bairro": "Teste",
        "municipio": "São Paulo",
        "uf": "SP",
        "cep": "01310-100"
      }
    },
    "servico": {
      "descricao": "Serviço de teste - Manager SaaS",
      "valor_frete": 100.00,
      "valor_total": 100.00
    }
  }'
```

### **4.2 Resposta da Emissão**

```json
{
  "status": "success",
  "message": "CT-e OS emitido com sucesso",
  "data": {
    "numero": "123456",
    "chave_acesso": "35231234567890123456789012345678901234567890",
    "protocolo": "123456789012345",
    "data_emissao": "2024-01-15T10:30:00Z",
    "status": "AUTORIZADO",
    "xml_url": "https://api.managersaas.com/v1/cte/os/123456/xml",
    "pdf_url": "https://api.managersaas.com/v1/cte/os/123456/pdf"
  }
}
```

## Passo 5: Monitorar o Status

### **5.1 Consulta de Status**

```bash
curl -X GET "https://api.managersaas.com/v1/cte/os/123456/status" \
  -H "Authorization: Bearer SEU_TOKEN"
```

### **5.2 Download dos Arquivos**

```bash
# Download do XML
curl -X GET "https://api.managersaas.com/v1/cte/os/123456/xml" \
  -H "Authorization: Bearer SEU_TOKEN" \
  -o cte_os_123456.xml

# Download do PDF
curl -X GET "https://api.managersaas.com/v1/cte/os/123456/pdf" \
  -H "Authorization: Bearer SEU_TOKEN" \
  -o cte_os_123456.pdf
```

## Passo 6: Configurar Monitorador de Pastas

### **6.1 Estrutura de Pastas**

Crie a seguinte estrutura no seu servidor:

```
/manager-saas/
├── entrada/
│   ├── cte/
│   ├── nfe/
│   └── nfce/
├── saida/
│   ├── processados/
│   ├── erros/
│   └── logs/
└── config/
    └── config.txt
```

### **6.2 Arquivo de Configuração**

Crie `config/config.txt`:

```txt
API_URL=https://api.managersaas.com/v1
TOKEN=seu_token_aqui
EMPRESA_ID=seu_empresa_id
AMBIENTE=production
INTERVALO_MONITORAMENTO=30
```

### **6.3 Arquivo de Entrada**

Crie um arquivo de teste em `entrada/cte/`:

```txt
TIPO_DOCUMENTO=CTE_OS
EMISSOR_CNPJ=12345678000199
EMISSOR_IE=123456789
EMISSOR_RAZAO_SOCIAL=SUA EMPRESA LTDA
TOMADOR_CNPJ=98765432000188
TOMADOR_RAZAO_SOCIAL=CLIENTE TESTE LTDA
SERVICO_DESCRICAO=Serviço via monitorador
SERVICO_VALOR_FRETE=150.00
SERVICO_VALOR_TOTAL=150.00
```

## Passo 7: Implementar em Produção

### **7.1 Tratamento de Erros**

```javascript
try {
  const response = await fetch('https://api.managersaas.com/v1/cte/os/emitir', {
    method: 'POST',
    headers: {
      'Authorization': `Bearer ${token}`,
      'Content-Type': 'application/json'
    },
    body: JSON.stringify(dados)
  });
  
  if (!response.ok) {
    throw new Error(`HTTP error! status: ${response.status}`);
  }
  
  const result = await response.json();
  console.log('Documento emitido:', result);
  
} catch (error) {
  console.error('Erro na emissão:', error);
  // Implementar retry logic ou notificação
}
```

### **7.2 Webhooks**

Configure webhooks para receber notificações em tempo real:

```bash
curl -X POST "https://api.managersaas.com/v1/webhooks/configurar" \
  -H "Authorization: Bearer SEU_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "url": "https://seu-site.com/webhook",
    "eventos": ["documento_autorizado", "documento_rejeitado"],
    "ativo": true
  }'
```

## Próximos Passos

<CardGroup cols={2}>
  <Card
    title="Documentação da API"
    href="/api/endpoints/emission"
    icon="code"
  >
    Explore todos os endpoints disponíveis
  </Card>
  <Card
    title="Exemplos de Integração"
    href="/examples/php"
    icon="terminal"
  >
    Veja exemplos práticos em diferentes linguagens
  </Card>
  <Card
    title="Tipos de Documentos"
    href="/documents/cte/overview"
    icon="file-text"
  >
    Conheça todos os documentos suportados
  </Card>
  <Card
    title="Suporte"
    href="mailto:suporte@managersaas.com"
    icon="message-circle"
  >
    Entre em contato com nossa equipe
  </Card>
</CardGroup>

## Solução de Problemas

### **Erro 401 - Não Autorizado**
- Verifique se o token está correto
- Confirme se o token não expirou
- Verifique as permissões da chave

### **Erro 400 - Dados Inválidos**
- Revise os dados enviados
- Verifique campos obrigatórios
- Confirme formatos de data e valores

### **Erro 500 - Erro Interno**
- Entre em contato com o suporte
- Verifique o status da API
- Aguarde alguns minutos e tente novamente

## Recursos Adicionais

- **Documentação Completa**: [docs.managersaas.com](https://docs.managersaas.com)
- **Status da API**: [status.managersaas.com](https://status.managersaas.com)
- **Suporte**: suporte@managersaas.com
- **Dashboard**: [dashboard.managersaas.com](https://dashboard.managersaas.com)

---

🎉 **Parabéns!** Você configurou com sucesso sua integração com o Manager SaaS. Agora pode começar a emitir documentos fiscais eletrônicos de forma automatizada e eficiente.
